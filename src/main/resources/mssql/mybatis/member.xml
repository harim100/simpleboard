<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mssql.mybatis.memberMapper">
<insert id="joinMember" parameterType="map" >
	INSERT INTO Cu_Customer 
	(
		Id
		,Pw
		,CustomerName
		,CellNum
	)
	VALUES
	( 
		#{Id}  
		,#{Pw}  
		,#{CustomerName}
		,#{CustomerPhone}
	)
</insert>

<select id="login" resultType="com.board.dto.MemberDto">
	SELECT 
		CustomerNum
		,Id
		,Pw
		,CustomerName
	FROM Cu_Customer WITH(NOLOCK)
	WHERE Id = #{Id} AND Pw = #{Pw}
</select>

<select id="loginBcrypt" resultType="com.board.dto.MemberDto">
	SELECT 
		CustomerNum
		,Id
		,Pw
		,CustomerName
	FROM Cu_Customer WITH(NOLOCK)
	WHERE Id = #{Id}
</select>

<select id="checkId" parameterType="map" resultType="int">
	SELECT COUNT(*) 
	FROM Cu_Customer WITH(NOLOCK)
	WHERE Id = #{requestedId}
</select>

</mapper>